version: 2.1
jobs:
  build:
    executor: sonar/default
    steps:
    - checkout
    - sonar/analyze
orbs:
  sonar: tom-vanbraband-sns/sonarcloud-remote-docker@dev:0.0.14
workflows:
  version: 2
  main:
    jobs:
      - build:
          context: test