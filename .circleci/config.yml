version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/node:4.8.2'
    steps:
    - checkout
    - sonar/analyze
orbs:
  sonar: tom-vanbraband-sns/scanner-no-docker@dev:0.0.6
workflows:
  version: 2
  main:
    jobs:
      - build:
          context: test